import type { HttpContext } from '@adonisjs/core/http'

export default class AshishesController {
  
  async index({}: HttpContext) {}
  
  async show({}: HttpContext) {}
  
  async store({}: HttpContext) {}
  
  async destroy({}: HttpContext) {}
  
}